#pragma once

#include "../data/data_definition.h"

using namespace DW::IMAGE;

namespace DW {
	namespace Render{

		//each imaging object is associated with each viewport
		class DWImaging 
		{
		public:
			//// 放缩
			//virtual void Zoom(int scale) = 0;
			//// 移动
			//virtual void Move(int distance) = 0;
			//// 绕着中心轴旋一定角度
			//virtual void Rotate(Vector3D axis, float angle) = 0;
			//// 窗宽窗位
			//virtual void WindowWidthLevel(int width, int level) = 0;
		protected:

		private:


		};
	}
}